# app/models/base.py

**用途**：提供所有 ORM model 的 `DeclarativeBase` 基底類別。

## 匯出

| 名稱 | 說明 |
|------|------|
| `Base` | `DeclarativeBase`，所有 model 均繼承此類別 |

## 被以下檔案 import

- `app/models/user.py`
- `app/models/market.py`
- `app/models/portfolio.py`
